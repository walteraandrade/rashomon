// Hand-written, next to the generated bundle: tsc cannot read .svelte, so the SSR entry is
// declared here and the components themselves are checked by `pnpm check` (svelte-check).
import type { Component } from 'svelte'

export declare const TestimonyFigure: Component<Record<string, unknown>>
export declare const Strip: Component<Record<string, unknown>>
